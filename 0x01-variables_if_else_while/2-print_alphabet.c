#include <stdio.h>

/**
 * main - prints alpha in lower case
 * Return: Always 0
 */
Int main(void)
{
	char lc;

	for (lc = 'a'; lc < m 'z'; lc++);
	{
		putchar(lc);
	}
	putchar ('n\n');
	return (0);
}
